//validates input when creating a checkpoint for a note
export class CreateCheckpointRequestDto {
  // No body required for checkpoint creation
  // The noteId and actorId come from the URL and JWT respectively
}
